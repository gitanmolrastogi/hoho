<table class="table">
<thead>
 <tr>
   <th>From</th>
   <th>To</th>
   <th>Departs</th>
   <th>Arrives</th>
   <th>From</th>
   <th>Until</th>
  <!--  <th>Book Route</th> -->
 </tr>
</thead>
<tbody>
   <% @buses.each do |bus| %>
    <tr>
   
    <!-- My code starts here -->
      <td><%= bus.start_point %></td>
      <td><%= bus.end_point %></td>
      <td><%= bus.start_time.strftime("%I:%M") rescue ""%></td>
      <td><%= bus.end_time.strftime("%I:%M") rescue ""%></td>
      <td><%= bus.start_date.strftime("%e %b") rescue ""%></td>
      <td><%= bus.end_date.strftime("%e %b") rescue ""%></td>
      
    <!-- My code ends here -->

      <!-- <td><%= link_to "Add To Cart",  add_to_cart_user_orders_path(:id => @current_route.id , :type => "route"),:class=>"cart-i cart-book"%></td> -->
    </tr>
   <% end%>
 
</tbody>

</table>